{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++: debug current file",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.out",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated",
      "preLaunchTask": "C++: build"
    }
  ]
}
